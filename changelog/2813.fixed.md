Fixed `recvmmsg` losing control messages when a `MultiHeaders` is reused: the
capacities in `msg_namelen`/`msg_controllen`, which the kernel overwrites with
the lengths it used, are now restored before every call.
